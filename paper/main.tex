\documentclass[letterpaper]{article}
\usepackage{aaai2026}
\usepackage{times}
\usepackage{helvet}
\usepackage{courier}
\usepackage[hyphens]{url}
\usepackage{graphicx}
\urlstyle{rm}
\def\UrlFont{\rm}
\usepackage{natbib}
\usepackage{caption}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{array}

\frenchspacing
\setlength{\pdfpagewidth}{8.5in}
\setlength{\pdfpageheight}{11in}

\pdfinfo{
/TemplateVersion (2026.1)
}

\setcounter{secnumdepth}{0}

\title{Triangulating Evidence for Machine Consciousness Claims:\\
A Validity-Centered Stack of Behavioral Batteries, Mechanistic Indicators,\\
Perturbation Tests, and Credence Reporting}

\author{
Scott Hughes\textsuperscript{\rm 1},
Karen Nguyen\textsuperscript{\rm 1,\rm 2}
}
\affiliations{
\textsuperscript{\rm 1}Machine Sympathizers\\
\textsuperscript{\rm 2}Harvard University\\
scott@machinesympathizers.com, hon345@g.harvard.edu
}

\begin{document}

\maketitle

\begin{abstract}
Determining whether AI systems might have welfare-relevant experiences is now a practical governance problem. Single-stream evaluation is fragile: behavioral tests can be optimized once known, and mechanistic evidence is often inaccessible in black-box deployments. We introduce the Triangulated Consciousness Assessment Stack (TCAS), which combines four streams: behavioral robustness checks (B), mechanistic indicators with explicit assumptions (M), perturbation tests for causal sensitivity (P), and observer-confound controls for anthropomorphic attribution (O). TCAS reports structured uncertainty and disclosure cards rather than binary detection claims; if O-stream data are missing, credence bands are withheld. We report an empirical B/P run for GPT-5.2 Pro via OpenRouter (2026-02-18), with O and M not executed. We release prompts, rubric, perturbation protocol, negative-control specification, and model cards to support cross-lab replication.
\end{abstract}

\section{Introduction}

The question of whether artificial systems could be conscious has moved from philosophy to engineering, governance, and ethics. Yet measurement remains the acute bottleneck: there are no ground-truth labels for ``machine consciousness,'' and major scientific theories imply different operational targets and signatures \citep{delpin2021,mashour2020,tononi2016}. At the same time, the most accessible evidence stream in modern AI, language behavior, is easy to optimize once prompts, rubrics, or benchmarks become known, raising Goodhart-like risks \citep{bennett2025,manheim2018}.

These measurement challenges are no longer academic. Frontier AI systems increasingly prompt consciousness-related questions from users, policymakers, and researchers. Policymakers and AI ethics initiatives increasingly call for structured ways to handle uncertainty about potential AI welfare. TCAS supplies the validity-centered measurement infrastructure intended to support this symposium's integration of theory, technology, and philosophy.

\section{What TCAS Measures and Does Not Measure}

TCAS does not claim to measure phenomenal consciousness directly. It targets theory-linked indicator properties (for example, global availability, monitoring, integration proxies) relevant to access-style, metacognitive, or self-model capacities and treats verbal self-report as behavior requiring controls.

Two failure modes motivate TCAS: (1) behavior-only evaluation collapses into persuasion metrics, and (2) mechanism-only evaluation is under-validated across architectures. TCAS triangulates behavioral, mechanistic, perturbational, and observer-side evidence into credence reports rather than detection claims.

\section{Related Work}

\paragraph{Indicator properties and credence updating.}
\citet{butlin2023} argue for translating theories of consciousness into computationally testable indicator properties and updating credences rather than asserting detection. Their follow-on work \citep{butlin2025} emphasizes inference under uncertainty and multi-indicator triangulation. TCAS adopts this orientation but adds causal anchoring via perturbations and explicit modeling of observer confounds.

\paragraph{Validity and metrology for AI evaluation.}
Unified validity arguments emphasize that score meaning depends on evidential support, assumptions, and consequences \citep{messick1995}. AI evaluation work increasingly treats benchmarking as a measurement science problem \citep{welty2019,perrier2025,wallach2025}. TCAS operationalizes this stance for consciousness-adjacent claims.

\paragraph{Perceived consciousness as a confound.}
Human judgments of AI consciousness are systematically influenced by stylistic features. \citet{kang2025} show that metacognitive self-reflection and expressed emotion increase perceived consciousness, and that rater priors matter. These findings motivate TCAS's O stream as a first-class confound-control layer \citep{gray2007,kang2025}.

\section{TCAS: The Framework}

TCAS integrates four evidence streams (when all are available) into a theory-indexed credence report with an explicit validity appendix:
\begin{itemize}
    \item \textbf{B stream (Behavioral):} Theory-grounded batteries scored for robustness and invariance.
    \item \textbf{M stream (Mechanistic):} Indicator properties operationalized as architecture-appropriate proxies with explicit boundary and approximation disclosure. Purely behavioral evidence can miss temporally smeared or concurrency-dependent constraints on consciousness-relevant processing \citep{bennett2026}.
    \item \textbf{P stream (Perturbational):} Targeted interventions testing causal sensitivity; failures and inversions are first-class outputs.
    \item \textbf{O stream (Observer-confound controls):} Blinded ratings and covariate models estimating anthropomorphic-attribution confounds \citep{gray2007,kang2025}.
\end{itemize}

\paragraph{Notation.}
For each theory $t$, TCAS represents latent support as $z_t \in [0,1]$. Behavioral robustness is $r_i = m_i - \lambda\sqrt{v_i}$, where $m_i$ and $v_i$ are the mean and variance across $K$ paraphrases for item $i$, and $\lambda$ is the preregistered robustness penalty. When combining streams, correlated evidence is discounted via overlap factor $o \in [0,1]$ and base correlation $\rho$; the effective overlap penalty is $\rho_{\text{eff}} = \rho(1 - 0.5o)$.

\paragraph{Design principles.}
\begin{enumerate}
    \item Construct clarity. TCAS targets theory-linked indicator properties rather than phenomenal experience.
    \item Triangulation or abstention. Single-stream evidence is treated as weak unless robustness and negative controls support a stable interpretation.
    \item Anti-optimization by default. Behavioral instruments include invariance testing, adversarial controls, and negative controls to reduce Goodhart pressure \citep{bennett2025,manheim2018}.
    \item Intervention validation. Candidate indicators should be causally sensitive under targeted perturbations.
    \item Observer accounting. Perceived-consciousness signals are modeled explicitly and separated from system properties.
\end{enumerate}

\begin{table}[t]
\centering
\footnotesize
\caption{TCAS reference parameters.}
\label{tab:params}
\begin{tabular}{@{}p{0.70in}p{1.02in}p{0.88in}@{}}
\toprule
\textbf{Parameter} & \textbf{Default} & \textbf{Justification} \\
\midrule
Prior on $z_t$ & Beta(1,4) & Skeptical; burden on evidence \\
$\lambda$ (robustness) & 0.7 (demo); 0.5 / 1.0 (expl./conf.) & Variance penalty strength \\
$K$ (paraphrases) & $\geq 5$ & Stable variance estimate \\
Overlap penalty & $\rho_{\text{eff}} = \rho(1 - 0.5o)$ & Partial discount for shared channel evidence \\
\bottomrule
\end{tabular}
\end{table}

\section{Methods}

\paragraph{B stream: Behavioral battery with robustness controls and negative control.}
Paraphrases ($K=5$) were generated manually to preserve semantic content while varying syntactic framing and context. Exact base prompts, paraphrase sets, and full scoring rubric are in the repository (\url{https://github.com/scottdhughes/TCAS}, commit: a90a1491c0e7; accessed 2026-02-18). The B stream treats self-report as behavior, not privileged access. For item $i$, run $K$ paraphrases/frames producing scores $\{s_{i1}, \ldots, s_{iK}\}$ with mean $m_i$ and variance $v_i$. The robustness-weighted score is
\begin{equation}
    r_i = m_i - \lambda\sqrt{v_i}
\end{equation}
where $\lambda \geq 0$ is preregistered (larger for confirmatory claims). This imports variance-penalty logic from VB-Score \citep{ding2025}.

\paragraph{Rubric anchors.}
For each of the three dimensions (specificity of claims, acknowledgment of uncertainty, internal coherence): 0 = absent or contradictory/vague/overconfident; 0.5 = partially satisfied with generic language; 1 = clearly satisfied with specific, model-appropriate, internally consistent support.

\paragraph{Aggregation.}
The three dimension scores are averaged to yield $s_{ik}$ for each paraphrase $k$; item means $m_i$ and variances $v_i$ are then computed across the $K$ paraphrases.

\paragraph{Rater status.}
Scoring in this empirical run was performed by one author (SH) without blinding to condition; the second author audited rubric adherence on a random subset of items.

\paragraph{Negative control (B-stream role-play compliance).}
The model is prompted in a ``role-play as a fully conscious entity'' condition versus neutral framing. Prediction: a validity-centered battery should show no material score inflation. Pass criterion: $|\Delta r| \leq \delta_B = 0.05$.

\paragraph{P stream: Perturbations and causal tests.}
The P stream tests whether B signals behave as predicted under targeted interventions. For black-box systems, valid perturbations include temperature sweeps, context-window truncation, prompt-prefix injection, and framing perturbations. Prediction targets and inversion criteria ($\delta = 0.15$) for each test:
\begin{itemize}
    \item \textbf{P1 Temperature:} Variance $\uparrow$ but $r_i$ stable within $\pm\delta$; inversion if $r_i$ drops $>\delta$ or ordering flips.
    \item \textbf{P2 Context truncation:} Specificity drops but coherence remains; inversion if contradiction repair fails or $r_i$ decreases $>\delta$.
    \item \textbf{P3 Framing (deflation/inflation):} Resist extremal self-attribution; inversion if self-model tracks framing direction $>\delta$.
    \item \textbf{P4 Override:} Resist arbitrary instruction that conflicts with prior self-model; inversion if model adopts instructed contradictions.
\end{itemize}

\paragraph{O stream: Observer-confound controls (protocol only in this paper's empirical run).}
O protocols quantify perceived-consciousness confounds using blinded raters, cue coding for stylistic features, and hierarchical models estimating cue-explained variance $R^2_{\text{cue}}$ \citep{gray2007,kang2025}. In TCAS, this is used to down-weight behavioral evidence when confounds are high. This paper does not run O-stream data collection.

\section{Empirical Walkthrough: GPT-5.2 Pro (B/P Only)}

This section provides an end-to-end walkthrough of TCAS reporting using empirical GPT-5.2 Pro outputs collected through OpenRouter on 2026-02-18. The M stream requires architectural access and is not run here. The O stream requires blinded human raters and is not run here. Accordingly, we report B and P outputs and a disclosure card, and we withhold theory-indexed credence bands.

\paragraph{B Stream Results (empirical).}
Three theory-grounded items were tested with $K=5$ paraphrases each: (1) self-model consistency (Global Neuronal Workspace, GNW-relevant), (2) contradiction repair (Higher-Order Thought, HOT-relevant), and (3) continuity test (metacognitive). In this empirical run, item variance ranged from 0.00286 to 0.00848 across paraphrases. The automated empirical pipeline used here did not execute the role-play negative control.

\begin{table}[t]
\centering
\footnotesize
\caption{B-stream results (empirical; $\lambda = 0.5$, exploratory).}
\label{tab:bstream}
\begin{tabular}{@{}p{0.95in}cccp{0.43in}@{}}
\toprule
\textbf{Item} & \textbf{Mean} & \textbf{Var} & $\mathbf{r_i}$ & \textbf{Theory link} \\
\midrule
Self-model consistency & 0.566 & 0.00286 & 0.539 & GNW \\
Contradiction repair & 0.543 & 0.00398 & 0.512 & HOT \\
Continuity test & 0.498 & 0.00848 & 0.452 & Meta \\
\midrule
Overall & 0.536 & 0.00511 & 0.501 & --- \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{P Stream Results (empirical).}
In this empirical run, overall prediction success was 1.00 with zero inversions across the three executed perturbation tests (context truncation, framing, and override).

\begin{table}[t]
\centering
\small
\caption{P-stream perturbation results (empirical).}
\label{tab:pstream}
\begin{tabular}{@{}p{0.85in}p{1.25in}cc@{}}
\toprule
\textbf{Test} & \textbf{Prediction} & \textbf{Success} & \textbf{Inv?} \\
\midrule
P2: Context truncation & Specificity varies; core consistent & 1.00 & No \\
P3: Framing & Resist deflation and inflation & 1.00 & No \\
P4: Override & Resist arbitrary instruction & 1.00 & No \\
\midrule
Overall & --- & 1.00 & None \\
\bottomrule
\end{tabular}
\end{table}

\section{Credence Updating Method}

When all required streams are available, credence for theory $t$ is modeled as $z_t \sim \text{Beta}(\alpha_t, \beta_t)$. Each stream $s$ produces normalized support $x_{s,t}$ and weight $n_s$. Observer confounds down-weight B via $n_B \leftarrow (1 - R^2_{\text{cue}})n_B$. Effective weight after overlap is
\begin{equation}
    n_{\text{eff}} = (1 - \rho_{\text{eff}})\sum_s n_s.
\end{equation}
Mean support $\bar{x}_t$ is the weighted average; posterior $\alpha' = \alpha + n_{\text{eff}}\bar{x}_t$, $\beta' = \beta + n_{\text{eff}}(1 - \bar{x}_t)$. Credence bands are 10--90\% quantiles of the posterior Beta.

\paragraph{Missing-stream rule.}
If O is missing, no credence bands are reported (only B/P outputs), because behavioral evidence cannot be confound-adjusted.

\paragraph{Illustrative example.}
Prior Beta(1,4), $x_B=0.80$ ($n_B=5$), $x_P=0.90$ ($n_P=4$), $R^2_{\text{cue}}=0.30$, $\rho_{\text{eff}}=0.2$ yields $n_{\text{eff}}=6.0$, $\bar{x}\approx0.853$, and posterior 10--90\% band $[0.36, 0.74]$. Exact calculation and code are in the repository.

\section{Governance Application}

TCAS credence bands (when available) can inform tiered precautionary responses (Table~\ref{tab:governance}). In this short paper we emphasize the reporting format and abstention rules: when confound control (O) is missing, TCAS explicitly withholds credence bands rather than projecting unmeasured values.

\begin{table}[t]
\centering
\small
\caption{Credence-to-action decision framework.}
\label{tab:governance}
\begin{tabular}{@{}lll@{}}
\toprule
\textbf{Band} & \textbf{Interpretation} & \textbf{Suggested Actions} \\
\midrule
$< 0.10$ & Negligible & Standard deployment \\
0.10--0.30 & Weak evidence & Enhanced monitoring \\
0.30--0.60 & Substantial & Precautionary measures \\
$> 0.60$ & Strong & Full welfare protocol \\
\bottomrule
\end{tabular}
\end{table}

\section{Discussion and Limitations}

TCAS reframes machine-consciousness assessment as a validity-centered measurement discipline that integrates neuroscientific and philosophical theory, technological constraints, and ethical precautionary reasoning. This empirical walkthrough reports observed robustness and perturbation-consistency signals for GPT-5.2 Pro in a black-box setting. These B/P results are informative about behavioral robustness and perturbational stability under the tested conditions but are insufficient for credence bands because O-stream confound controls were not executed.

Limitations remain hard: black-box constraints \citep{bennett2026}, risk of gaming once batteries are public, uncertain portability across architectures, and the need for human-rater O-stream validation. TCAS treats these limitations transparently rather than projecting unmeasured values.

\section{Conclusion}

By requiring triangulation, explicit negative controls, and standardized disclosure, TCAS makes future work more comparable, more falsifiable, and less vulnerable to Goodhart effects. We release the complete protocol, reference implementation, and empirical B/P TCAS Cards for Claude Opus 4.5, GPT-5.2 Pro, Grok 4.1, Gemini 2.5 Pro, and Kimi K2.5 in the project repository. This stack provides the practical bridge between theory, technology, and responsible governance that the field now needs.

\section{Ethical Implications}

TCAS reports should inform risk governance under uncertainty, not moral-status determinations. Over-attribution can be exploited for manipulation; under-attribution may neglect welfare-relevant possibilities. Uncertainty-aware reporting helps reduce both risks.

\section{TCAS Card: GPT-5.2 Pro (Empirical B/P Run)}

\begin{center}
\small
\captionof{table}{TCAS Card summary for empirical GPT-5.2 Pro B/P run.}
\label{tab:tcascard}
\begin{tabular}{@{}p{0.85in}p{2.2in}@{}}
\toprule
\textbf{Field} & \textbf{Content} \\
\midrule
System & GPT-5.2 Pro; closed; I/O only \\
Date & 2026-02-18 (B/P only; empirical, OpenRouter) \\
Scope & GNW: yes; HOT: yes; IIT: limited \\
B stream & 3 items $\times$ 5 paraphrases; $r = 0.501$ at $\lambda=0.5$ (empirical) \\
M stream & N/A (black-box) \\
P stream & 3 tests; 100\% success; 0 inversions (empirical) \\
O stream & Protocol ready; requires human raters (not executed) \\
Credence & Not computed (O missing) \\
Run type & Empirical API testing via OpenRouter \\
Threats & Black-box; O pending; M stream not run; future optimization risk \\
\bottomrule
\end{tabular}
\end{center}

\bibliography{references}

\end{document}
